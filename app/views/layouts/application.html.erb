<!DOCTYPE html>
<html>
  <head>
    <title>MyDiscogsApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", media: "all" %>
    <style>
      input[type="email"], input[type="password"], input[type="text"], textarea {
        border: 1px solid #ccc !important;
        background-color: white !important;
        color: black !important;
        padding: 8px;
        border-radius: 4px;
        width: 100%;
        margin-top: 4px;
        display: block;
      }
    </style>
  </head>

  <footer class="mt-20 py-10 border-t border-gray-200 text-center text-sm text-gray-500">
    <div class="flex justify-center gap-6 mb-4">
      <%= link_to "利用規約", static_pages_terms_path, class: "hover:underline" %>
      <%= link_to "プライバシーポリシー", static_pages_privacy_path, class: "hover:underline" %>
    </div>
    <p>&copy; 2026 MyDiscogsApp</p>
  </footer>

  <body>
    <nav>
    <% if user_signed_in? %>
      <%# ログインしている時 %>
      <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %>
    <% else %>
      <%= link_to "新規登録", new_user_registration_path %> |
      <%= link_to "ログイン", new_user_session_path %>
    <% end %>
  </nav>
    <% if notice %>
      <p class="notice p-4 mb-4 text-sm text-green-800 rounded-lg bg-green-50"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert p-4 mb-4 text-sm text-red-800 rounded-lg bg-red-50"><%= alert %></p>
    <% end %>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
